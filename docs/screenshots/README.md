# Screenshots

These SVG files are visual examples for the public template.

- `notion-workflow-approval.svg` shows a Notion approval flow with a batched workflow job.
- `chatgpt-connector.svg` shows a ChatGPT connector setup with URL and auth fields.
- `signal-layer.svg` shows the distilled signal view for noisy job output.

If you want to replace these with real captures later, keep the same filenames so the README embeds do not need to change.
